import { LengthDirective } from './length.directive';

describe('LengthDirective', () => {
  it('should create an instance', () => {
    const directive = new LengthDirective();
    expect(directive).toBeTruthy();
  });
});
